import MovieDetails from "../Components/MovieDetails";

function Show() {
    return (
        <div className="container">
            <MovieDetails/>   
        </div>
    );
}

export default Show;
